services:
  - type: web
    name: probettips
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "PYTHONPATH=src uvicorn api:app --host 0.0.0.0 --port 10000"
    autoDeploy: true
